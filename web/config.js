/* Runtime configuration.
   Edit this file after deploying the API, or let Vercel rewrite it at build time. */
window.TALAN_CONFIG = {
  // Your Railway API base URL, with no trailing slash.
  apiBase: "https://REPLACE-ME.up.railway.app",
  // Email domain permitted to register.
  emailDomain: "talan.com"
};
